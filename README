Your assignment is to create a class that can be used to create a web page.  The web page class should be extended with an article class, header with jquery and css file include.  The class should also be able to write out the entire web page into a file.  The write process should be called cache and will eventually be used as a means of improving the applicationsn performance.
